---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";

export interface Props {
  site: CollectionEntry<"showcase">;
}

const { site } = Astro.props;
const widths = [450, 800];
const sizes = "(min-width: 1024px) 33vw, (min-width: 768px) 50vw, 100vw";
---

{/* 'reveal-card': Clase para nuestro script de aparición.
  'group': Permite controlar los hijos cuando hacemos hover en el padre.
*/}
<a 
  class="reveal-card group block relative overflow-hidden rounded-xl bg-offset shadow-lg transition-all duration-300 hover:shadow-2xl hover:-translate-y-1" 
  href={site.data.url}
>
  
  {/* Contenedor de Imagen con Ratio Fijo (Aspect Video) */}
  <div class="aspect-video overflow-hidden">
    <Image
      class="h-full w-full object-cover transition-transform duration-500 group-hover:scale-110"
      src={site.data.image}
      widths={widths}
      sizes={sizes}
      alt={`Screenshot of ${site.data.title}`}
    />
  </div>

  {/* Overlay (Capa oscura) con la información - Aparece en Hover */}
  <div class="absolute inset-0 bg-gradient-to-t from-black/90 via-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex flex-col justify-end p-6">
    
    <div class="transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300">
      <h3 class="text-xl font-bold text-white mb-2">
        {site.data.title}
      </h3>
      <p class="text-sm text-gray-200 line-clamp-2 mb-4">
        {site.data.description}
      </p>
      <span class="inline-block text-xs font-bold text-primary uppercase tracking-wider border-b-2 border-primary pb-1">
        View Project &rarr;
      </span>
    </div>

  </div>
</a>

<style>
  /* Estados iniciales para la animación de entrada */
  .reveal-card {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  /* Estado final (cuando el script añade esta clase) */
  .reveal-card.reveal-visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>